import styled from "styled-components";
import{Route as Sroutes} from "react-router-dom";

export const AppWrapper = styled.div`
    display: flex;
    width: 500px;
    height: 300px;
    position:relative;
`;
